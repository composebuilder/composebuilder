# 更新日志

## v2026.3.28.0

1. 修复导入 `healthcheck.test: ["NONE"]` 时被错误生成为空命令健康检查的问题
2. 修复可能生成空 `ports`/`volumes` 节点导致 YAML 不完整的问题
3. 修复环境变量导入对 `=` 的截断问题
4. 修复 `depends_on` 条件丢失问题，并保留对象形式条件配置
5. 修复镜像名称解析对带端口镜像仓库（如 `localhost:5000/...`）的兼容问题
6. 修复自定义健康检查命令为空时输出无效健康检查配置的问题
7. 增强 `command`、`environment`、`healthcheck.test` 的 YAML 双引号转义，避免特殊字符破坏配置
8. 调整环境变量导出规则，允许输出 `KEY=` 空值变量（仍拦截空键）
9. 修复导入 `user: "1000"` 时用户信息丢失的问题
10. 修复导入环境变量对象时 `null` 被错误转换为字符串 `"null"` 的问题
11. 修复导入未声明 `restart` 的服务时被强制改为 `restart: no` 的配置漂移问题
12. 修复导入数组形式 `command` 时语义被破坏的问题，现可按数组形式回写
13. 修复服务名与命名卷名在包含特殊字符时输出为无效 YAML key 的问题，现统一做 key 安全序列化
14. 修复 `image`、`container_name` 等字符串字段未安全转义导致的 YAML 破坏风险
15. 为 `restart` 增加合法值校验，避免导出非法策略值
16. 修复 `ports: ["8080"]` 导入后被错误改写为固定主机端口映射的问题，保留“仅容器端口”语义
17. 增强 `ports` 长语法对象（`target/published/protocol/host_ip`）导入兼容性
18. 增强 `volumes` 长语法对象导入兼容性，并支持匿名卷目标导出

## v2026.3.6.0

1. 增加深色模式支持
2. 修复丢失host_ip的问题
3. 修复Windows的volume解析错误问题
4. 修复depends_on条件丢失问题
5. 移除默认输出user: "0:0"

## v2026.1.31.1

1. 添加文档、Docker Hub、作者与仓库链接
2. 优化文本表述与链接样式
3. 本地化js组件与文本组件
4. 修复“正在编辑”的刷新问题

## v2026.1.29.1

1. 发布首个版本
